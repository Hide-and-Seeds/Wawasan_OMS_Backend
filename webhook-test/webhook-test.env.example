# Copy this file to  webhook-test.env  and paste your secret below.
# (Or just run Run-Test.cmd once - it can ask for the secret and save it for you.)
# webhook-test.env is git-ignored, so the secret never leaves this PC.

WEBHOOK_URL=https://wawasan-oms-backend.vercel.app/api/orders/webhook/sql-account
WEBHOOK_SECRET=paste-your-secret-here

{
  "name": "sql-account-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "optionalDependencies": {
    "node-firebird": "^1.1.8"
  },
  "scripts": {
    "start": "node --env-file=.env bridge.mjs",
    "dry-run": "node --env-file=.env bridge.mjs --dry-run"
  }
}
